name: ☁️ CloudCat TG 频道自动签到

on:
  # 1. 手动触发
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # cron 格式: 分 时 日 月 周

jobs:
  sign_in_job:
    runs-on: ubuntu-latest
    
    env:
      TG_API_ID: ${{ secrets.TG_API_ID }}
      TG_API_HASH: ${{ secrets.TG_API_HASH }}
      TG_CHANNEL: ${{ vars.TG_CHANNEL }} # 频道名/ID (可选，已在脚本中设置默认值)
      TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}

    steps:
    - name: ⬇️ 检出代码 (Checkout repository)
      uses: actions/checkout@v4
      
    - name: 🐍 安装 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        
    - name: 📦 安装 Python 依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r cloudcat-tgcheckin/requirements.txt
        
    - name: 🚀 执行 TG 频道签到脚本
      run: python cloudcat-tgcheckin/checkin.py
      
